FROM python:3.12-slim-bookworm

ENV HF_HOME=/models/huggingface \
    OMP_NUM_THREADS=5 \
    MKL_NUM_THREADS=5 \
    OPENBLAS_NUM_THREADS=5 \
    NUMEXPR_NUM_THREADS=5 \
    PYTHONUNBUFFERED=1

RUN apt-get update && apt-get install -y --no-install-recommends libsndfile1 ffmpeg && \
    rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
RUN pip install --no-cache-dir qwen-asr==0.0.6

WORKDIR /bench
COPY backend_common.py qwen_runner.py ./
ENTRYPOINT ["python", "/bench/qwen_runner.py"]
